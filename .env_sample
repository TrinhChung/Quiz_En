# Mẫu file môi trường (rút gọn, chỉ chứa các biến cần thiết)
# Sao chép file này thành `.env` và đặt giá trị thực tế (không commit secrets vào git).

# Môi trường ứng dụng: development hoặc production
FLASK_ENV=development
FLASK_APP=run.py

# Secret (bắt buộc) - thay bằng chuỗi đủ mạnh
SECRET_KEY=change_me_to_a_secure_value

# CSDL (mặc định: SQLite local)
DATABASE_URL=sqlite:///instance/app.db

# Cổng chạy ứng dụng (mặc định 5000)
PORT=5000

# Gợi ý: tạo SECRET_KEY an toàn:
# python -c "import secrets; print(secrets.token_hex(32))"
